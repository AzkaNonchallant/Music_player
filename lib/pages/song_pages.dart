import 'package:flutter/material.dart';

class SongPages extends StatelessWidget {
  const SongPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("S O N G  P A G E S"),
      ),
    );
  }
}